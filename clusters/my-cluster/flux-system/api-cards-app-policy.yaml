---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: api-cards-app
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: api-cards-app
  policy:
    semver:
      range: 5.0.x
